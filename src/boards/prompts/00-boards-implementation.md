# Boards Feature Prompt

## 📋 목표
- 문화예술인 게시판(목록/상세/작성) UI와 로컬 데이터 처리 흐름 정의
- `/boards`, `/boards/new`, `/boards/:id` 라우팅 및 페이지 컴포넌트 구성
- 샘플 데이터 + 로컬스토리지 기반 CRUD 흐름 문서화

## 🧱 구성
```
boards/
├── data.ts                      # 샘플 데이터 + storage helper
├── pages/
│   ├── BoardListPage.tsx        # 검색/필터 + 카드 리스트
│   ├── BoardDetailPage.tsx      # 게시글 상세(작성자 정보 포함)
│   └── BoardNewPage.tsx         # 신규 게시글 작성 폼
├── page.tsx                     # export default BoardListPage (라우팅용)
├── new/page.tsx                 # export default BoardNewPage (라우팅용)
├── [id]/page.tsx                # export default BoardDetailPage (라우팅용)
└── BoardsRouter.tsx             # /boards/* 라우터 설정
```

## ✅ 요구 사항
1. **라우팅**
   - `RootApp`에서 `/boards/*` 경로를 `BoardsRouter`로 위임
   - `BoardsRouter` 내에서 목록/작성/상세 라우트 구성

2. **게시글 목록**
   - 검색어 필터 + 태그 필터
   - 카드 형태로 게시글 요약/태그/연락처 노출
   - 새 글 작성 버튼 / 새로고침 버튼 / 홈 이동 버튼 포함

3. **게시글 상세**
   - 커버 이미지, 제목, 요약, 본문 표시
   - 작성자 정보 카드(작성자, 소속, 연락 이메일, 외부 링크)
   - 목록/뒤로 가기 버튼 제공

4. **새 게시글 작성**
   - 제목, 요약, 본문, 카테고리, 태그, 연락처 등 입력 폼
   - 최소 필수 값 검증
   - 저장 시 `data.ts` 헬퍼를 통해 로컬스토리지에 기록 후 상세 페이지로 이동

5. **테스트 페이지**
   - `/boards/test-connection`은 Supabase 연결 테스트용 유지

## 🔧 데이터 / 상태
| 파일 | 역할 |
| --- | --- |
| `data.ts` | 샘플 게시글과 로컬스토리지 read/write helper 제공 |
| `pages/BoardListPage.tsx` | `getBoardPosts()` 호출, 필터링, 카드 렌더 |
| `pages/BoardDetailPage.tsx` | `useParams`로 id 추출 → `getBoardById()` |
| `pages/BoardNewPage.tsx` | 폼 state 관리 → `addBoardPost()` |

## 🧪 테스트 체크리스트
- [ ] `/boards` 접속 시 목록 표시 및 필터 동작 확인
- [ ] `/boards/new`에서 글 작성 후 자동으로 상세 페이지 이동
- [ ] `/boards/{id}`에서 작성한 내용 정상 표시
- [ ] `/boards/test-connection`에서 Supabase 연결 상태 확인


